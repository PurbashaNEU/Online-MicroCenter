package com.neu.command_pattern;

//Defines the execute() method that all command objects must implement.
//Performs a specific operation when called.
public interface CommandAPI {
    String execute();
}
